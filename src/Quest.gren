module Quest exposing (..)

import Duration exposing (Duration)
import Quest.Requirement exposing (Requirement)
import Stat exposing (Stat)


type alias Quest =
    { title : String
    , requirements : Array Requirement
    , duration : Duration
    }
